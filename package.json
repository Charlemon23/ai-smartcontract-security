{
  "name": "ai-smartcontract-security",
  "version": "1.2.0",
  "description": "AI-Enhanced Smart Contract Security Testbed — offline dataset analysis, reproducible static scans, and human-readable reports.",
  "type": "module",
  "main": "scripts/run_pipeline.js",
  "scripts": {
    "run:pipeline": "node scripts/run_pipeline.js",
    "import:dataset": "node scripts/import_dataset.js",
    "summarize:reports": "node scripts/summarize_reports.js",
    "clean": "rm -rf data/reports/* data/contracts/offline_seed/* || true",
    "start": "npm run run:pipeline",
    "test": "echo \"No tests yet\""
  },
  "dependencies": {
    "axios": "^1.7.4",
    "chalk": "^5.3.0",
    "dotenv": "^16.4.5",
    "extract-zip": "^2.0.1",
    "fs-extra": "^11.2.0",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "eslint": "^9.10.0",
    "prettier": "^3.3.3"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "MIT"
}