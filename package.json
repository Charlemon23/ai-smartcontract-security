{
  "name": "ai-smartcontract-security",
  "version": "1.1.0",
  "description": "AI-Enhanced Smart Contract Security Testbed — offline dataset analysis and ML-ready pipeline.",
  "main": "scripts/run_pipeline.js",
  "type": "module",
  "scripts": {
    "start": "npm run run:pipeline",
    "run:pipeline": "node scripts/run_pipeline.js",
    "import:dataset": "node scripts/import_dataset.js",
    "analyze:single": "node scripts/analyze_single.js",
    "clean": "rm -rf data/contracts/imported data/reports/* tmp/* || true",
    "lint": "eslint . --ext .js",
    "test": "echo 'No tests defined yet.'"
  },
  "keywords": [
    "smart-contract",
    "security",
    "ethereum",
    "solidity",
    "static-analysis",
    "machine-learning",
    "dataset"
  ],
  "author": "Charlemon London",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.7.4",
    "chalk": "^5.3.0",
    "dotenv": "^16.4.5",
    "extract-zip": "^2.0.1",
    "fs-extra": "^11.2.0",
    "prettier": "^3.3.3"
  },
  "devDependencies": {
    "eslint": "^9.10.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
